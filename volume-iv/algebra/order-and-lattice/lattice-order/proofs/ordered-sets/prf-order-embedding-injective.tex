% =========================================================
% Proof: Every Order-Embedding Is Injective
% =========================================================

\newpage
\phantomsection
\label{prf:order-embedding-injective}

\begin{remark*}[Return]
\hyperref[lem:order-embedding-injective]{Return to Lemma~\ref*{lem:order-embedding-injective}.}
\end{remark*}

\begin{theorem*}[Every order-embedding is injective]
Let $(P, \leq_P)$ and $(Q, \leq_Q)$ be partially ordered sets and let
$\varphi : P \to Q$ be an order-embedding.  Then $\varphi$ is injective.
\end{theorem*}

\begin{proof}[Proof]
Suppose $\varphi(x) = \varphi(y)$.  Then
\[
\varphi(x) \leq_Q \varphi(y) \quad\text{and}\quad \varphi(y) \leq_Q \varphi(x).
\]
Since $\varphi$ is an order-embedding, the biconditional
$x \leq_P y \Longleftrightarrow \varphi(x) \leq_Q \varphi(y)$ holds for
all $x, y \in P$.  Order-reflection therefore gives
\[
x \leq_P y \quad\text{and}\quad y \leq_P x.
\]
By anti-symmetry of $\leq_P$, it follows that $x = y$.
\end{proof}

\begin{remark*}[Proof structure]
A direct proof by chasing the biconditional in the definition of
order-embedding.  The only content is anti-symmetry, applied once.
\end{remark*}

\begin{remark*}[Dependencies]
\hyperref[def:order-embedding]{Order-embedding},
\hyperref[def:lattice-order-partial-order]{anti-symmetry of partial orders}.
\end{remark*}
