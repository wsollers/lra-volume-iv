% =========================================================
% common/volume-preamble.tex
% Shared preamble for standalone volume and book root files.
%
% Usage -- in any volume-{roman}.tex or volume-{roman}-{book}.tex:
%   \documentclass[12pt, a4paper]{report}
%   \input{common/volume-preamble}
%   \begin{document}
%   ...
%   \end{document}
%
% Rule: shared package and macro changes belong here, in lra-common.
% =========================================================

% ── Shared infrastructure ─────────────────────────────────
\input{common/preamble}
\input{common/boxes}
\input{common/breadcrumb-macros}
\input{common/colors}
\input{common/figures-macros}
\input{common/structural-presentations}
\input{common/environments}
\input{common/macros}

% ── Hyperlinks and cross-references ───────────────────────
\usepackage{hyperref}
\usepackage{xr-hyper}
\usepackage{xstring}
\usepackage[nameinlink,capitalize,noabbrev]{cleveref}
\usepackage{bookmark}

\makeatletter
\newcommand{\LRAExternalDocumentIfExists}[1]{%
  \begingroup
  \edef\LRA@candidate{#1}%
  \edef\LRA@current{build/\jobname}%
  \IfStrEq{\LRA@candidate}{\LRA@current}{}{%
    \IfFileExists{#1.aux}{\externaldocument{#1}}{}%
  }%
  \endgroup
}
\newcommand{\LRAExternalVolumeDocumentIfExists}[2]{%
  \IfBeginWith{\jobname}{#1}{}{%
    \LRAExternalDocumentIfExists{#2}%
  }%
}
\newcommand{\LRAExternalRootDocumentIfExists}[2]{%
  \IfStrEq{\jobname}{#1}{}{%
    \LRAExternalDocumentIfExists{#2}%
  }%
}
\newcommand{\LRALoadExternalReferences}{%
  % Cross-volume references use sibling volume-level aux files when present.
  \LRAExternalVolumeDocumentIfExists{volume-i}{../lra-volume-i/build/volume-i}%
  \LRAExternalVolumeDocumentIfExists{volume-i}{/lra-repos/lra-volume-i/build/volume-i}%
  \LRAExternalVolumeDocumentIfExists{volume-ii}{../lra-volume-ii/build/volume-ii}%
  \LRAExternalVolumeDocumentIfExists{volume-ii}{/lra-repos/lra-volume-ii/build/volume-ii}%
  \LRAExternalVolumeDocumentIfExists{volume-iii}{../lra-volume-iii/build/volume-iii}%
  \LRAExternalVolumeDocumentIfExists{volume-iii}{/lra-repos/lra-volume-iii/build/volume-iii}%
  \LRAExternalVolumeDocumentIfExists{volume-iv}{../lra-volume-iv/build/volume-iv}%
  \LRAExternalVolumeDocumentIfExists{volume-iv}{/lra-repos/lra-volume-iv/build/volume-iv}%
  \LRAExternalVolumeDocumentIfExists{volume-v}{../lra-volume-v/build/volume-v}%
  \LRAExternalVolumeDocumentIfExists{volume-v}{/lra-repos/lra-volume-v/build/volume-v}%
  \LRAExternalVolumeDocumentIfExists{volume-vi}{../lra-volume-vi/build/volume-vi}%
  \LRAExternalVolumeDocumentIfExists{volume-vi}{/lra-repos/lra-volume-vi/build/volume-vi}%
  \LRAExternalVolumeDocumentIfExists{volume-vii}{../lra-volume-vii/build/volume-vii}%
  \LRAExternalVolumeDocumentIfExists{volume-vii}{/lra-repos/lra-volume-vii/build/volume-vii}%
  \LRAExternalVolumeDocumentIfExists{volume-viii}{../lra-volume-viii/build/volume-viii}%
  \LRAExternalVolumeDocumentIfExists{volume-viii}{/lra-repos/lra-volume-viii/build/volume-viii}%
  % Same-volume book references use local book aux files when present.
  \LRAExternalRootDocumentIfExists{volume-i-set-theory}{build/volume-i-set-theory}%
  \LRAExternalRootDocumentIfExists{volume-i-mathematical-logic-and-proof}{build/volume-i-mathematical-logic-and-proof}%
  \LRAExternalRootDocumentIfExists{volume-i-foundational-geometry}{build/volume-i-foundational-geometry}%
  \LRAExternalRootDocumentIfExists{volume-ii-discrete-number-systems}{build/volume-ii-discrete-number-systems}%
  \LRAExternalRootDocumentIfExists{volume-ii-the-continuum}{build/volume-ii-the-continuum}%
  \LRAExternalRootDocumentIfExists{volume-iii-bounds-sequences-and-series}{build/volume-iii-bounds-sequences-and-series}%
  \LRAExternalRootDocumentIfExists{volume-iii-continuity}{build/volume-iii-continuity}%
  \LRAExternalRootDocumentIfExists{volume-iii-differentiation}{build/volume-iii-differentiation}%
  \LRAExternalRootDocumentIfExists{volume-iii-integration}{build/volume-iii-integration}%
  \LRAExternalRootDocumentIfExists{volume-iv-mathematical-spaces}{build/volume-iv-mathematical-spaces}%
  \LRAExternalRootDocumentIfExists{volume-v-measure-theory}{build/volume-v-measure-theory}%
  \LRAExternalRootDocumentIfExists{volume-v-probability}{build/volume-v-probability}%
  \LRAExternalRootDocumentIfExists{volume-v-complex-analysis}{build/volume-v-complex-analysis}%
  \LRAExternalRootDocumentIfExists{volume-v-functional-analysis}{build/volume-v-functional-analysis}%
  \LRAExternalRootDocumentIfExists{volume-vi-algebra}{build/volume-vi-algebra}%
  \LRAExternalRootDocumentIfExists{volume-vi-linear-algebra}{build/volume-vi-linear-algebra}%
  \LRAExternalRootDocumentIfExists{volume-vi-lattice-and-order-theory}{build/volume-vi-lattice-and-order-theory}%
  \LRAExternalRootDocumentIfExists{volume-vii-proof-theory}{build/volume-vii-proof-theory}%
  \LRAExternalRootDocumentIfExists{volume-vii-model-theory}{build/volume-vii-model-theory}%
  \LRAExternalRootDocumentIfExists{volume-vii-type-theory}{build/volume-vii-type-theory}%
  \LRAExternalRootDocumentIfExists{volume-vii-category-theory}{build/volume-vii-category-theory}%
  \LRAExternalRootDocumentIfExists{volume-vii-lambda-calculus}{build/volume-vii-lambda-calculus}%
  \LRAExternalRootDocumentIfExists{volume-viii-numerical-foundations}{build/volume-viii-numerical-foundations}%
  \LRAExternalRootDocumentIfExists{volume-viii-applied-methods}{build/volume-viii-applied-methods}%
}
\AtBeginDocument{\LRALoadExternalReferences}
\makeatother

% ── cleveref configuration ────────────────────────────────
\crefname{theorem}{Theorem}{Theorems}
\Crefname{theorem}{Theorem}{Theorems}
\crefname{claim}{Claim}{Claims}
\Crefname{claim}{Claim}{Claims}
\crefname{subclaim}{Subclaim}{Subclaims}
\Crefname{subclaim}{Subclaim}{Subclaims}

\hypersetup{
  colorlinks = true,
  linkcolor  = blue!60!black,
  citecolor  = blue!60!black,
  urlcolor   = blue!60!black,
}

% Allow \hyperref inside theorem optional arguments.
\pdfstringdefDisableCommands{%
  \def\hyperref[#1]#2{#2}%
}

% ── Indexes ───────────────────────────────────────────────
\usepackage{imakeidx}
\makeindex[name=tech,title={Technique Index}]
\makeindex[name=dep,title={Dependency Index}]

% ── Numbering and TOC depth ───────────────────────────────
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}
